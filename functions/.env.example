# Local-only fallbacks. Production secrets live in Google Cloud Secret Manager
# (security finding F2.1). Copy this file to `.env` for local emulator runs.
OPENAI_API_KEY=sk-replace-me
AI_MODEL_VERDICT=gpt-4o-mini
AI_MODEL_SAFETY=gpt-4o-mini
PROMPT_VERSION=v1.0.0
REVENUECAT_WEBHOOK_SECRET=replace-me
